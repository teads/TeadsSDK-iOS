# Xcode
#
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
package.xcworkspace
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
.DS_Store

# CocoaPods
#
# The podspecs are still the published CocoaPods distribution, so keep ignoring
# the Pods directory of anyone integrating them locally.
Pods/

# Swift Package Manager
#
# `Package.resolved` is intentionally NOT ignored: the workspace one pins the
# SDK versions the sample apps are validated against.
.build/
.swiftpm/
.serena
